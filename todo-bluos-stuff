Test tool open firewall:

ZONE=$(sudo firewall-cmd --get-zone-of-interface=wlp2s0)
sudo firewall-cmd --zone="$ZONE" --add-port=11430/udp --timeout=5m

-----

I think it makes sense to pre-extract code to make the chat cheaper

-----

Look for more third-party projects on GitLab. The repackaged Linux controller
(gitlab.com/zquestz/bluos-controller-linux) was found there and not on GitHub,
so the search so far has been GitHub-heavy. There may be more.

-----

Decide what to do with the ChatGPT/AI analysis idea for finding new music.
ibeex/blue_cli (github.com/ibeex/blue_cli) does something along those lines
and is the closest existing example found.

-----

Compare the problems documented here with the first Claude chat about the
BluOS Android app's behaviour, to see what was already suspected then and
what has since been settled or overturned:
https://claude.ai/chat/f9b68d13-ae8f-4a09-9190-931757f92cbe

-----

Build a proof-of-concept discovery-only Android app, to be able to show
Bluesound "it can be done much better, see here". Open questions to explore
first:

- How fast is the fastest possible discovery? There is a lot of LSDP data in
  bluos-api/ already; mDNS data is missing entirely.
- Is Android's own mDNS stack (NsdManager) any better in practice than what
  the BluOS Controller app does today?

-----

Reconsider Roon.

- Check it against the current list of requirements.
- Check whether HDMI playback in Stue works with Roon.
- Work out what the problem was the last time it was tried.

-----

The old forum API reference several projects build on: the archived link is
saved, in musica/ECOSYSTEM.md under Honourable mentions. The live
helpdesk.bluesound.com thread is gone; the entry already points at the web
archive snapshot instead.

What is missing is the link where it is actually used. bluos-api cites this
source as "2015 forum" for several claims (C-10, C-32 among them) without a
URL, so the reference for those rows lives in a different folder under a
different name. Worth putting the archive URL next to the source name there.

-----

The app sometimes opens with a different player selected than the one in use
when it was closed, which forces another trip through discovery. Check the
source for this one the way the other problems were checked — it was not among
them, so nobody has looked at why the stored selection is not honoured.

-----

Try grouping and ungrouping on the iOS app, and see whether any of the problems
reproduce there. Then use it as the everyday controller for a while: the
comparison so far is about discovery, and it says nothing about whether iOS is
actually pleasant to live with.

-----

Follow up on the Pixel 3a XL rows in bluos-api (R10, R11). Two things are
recorded as unproven there and both are cheap to settle: run `adb shell ps`
after a swipe away under each battery profile, which should say whether the
process survives and so explain why relaxing the restrictions makes the
everyday case worse; and pin down whether that phone's slower initial
discovery screen really does sometimes keep what it found.

-----

Settle player identity across interfaces. `bluos-api/SPEC-ADDITIONS-identity.md`
says a player has two interfaces, two MACs and two IPs, only one live at a
time, and that which one is live changes on its own. Three things are open
there:

- The test, which answers both open claims in one run: on a player that has
  run both ways, record `/SyncStatus` `id` and `mac` plus the LSDP `nodeId` on
  Ethernet, pull the cable, let it join wireless, and repeat. That says whether
  `mac` follows the live interface and whether `nodeId` is stable.
- The vendor pages behind the hardware behaviour were read through search
  result summaries, not opened — support1.bluesound.com is unreachable from
  the machine that file was written on. Open them and check the wording before
  any of it is relied on.
- `C-56-syncstatus-mac-interface` and `C-57-lsdp-nodeid-interface-stable` exist
  only as proposals in that file; the register in `bluos-http-api.md` still
  ends at C-55. Fold them in once the test and the sources are done.

This one matters more than it looks: anything that dedupes players on `id`, or
on whatever address it reached a player at, turns one player into two the first
time somebody moves it between wired and wireless, and keeps a stale entry it
can never reach again.

-----

Fold the spec additions into `bluos-api/bluos-http-api.md`. Three documents
sit beside the spec holding content that belongs inside it, and each one is
findable only if you already know it exists:

- `SPEC-ADDITIONS-grouping.md` — what the official apps actually do when
  grouping, and the consequences for a client. Its headline finding, the
  mutual master loop, is already in §5.3 and in `C-39`; what is left is the
  rest of the file.
- `SPEC-ADDITIONS-identity.md` — the `mac` note for §2.1 and two register
  rows. This one has a precondition: see the identity entry above, the claims
  are untested and the vendor sources have not been opened.
- `SPEC-SECTION-claim-register.md` — a replacement §17 that is append-only
  with permanent claim ids, so a verified claim keeps its row instead of
  vanishing and taking the negative result with it.

`HANDOVER.md` has an "Open work" list that belongs with this, and it is the
larger half:

- A dozen claims are tested in the hardware `FINDINGS.md` but never written
  into §17.1 or the body — `C-07` through `C-15`, `C-18`, `C-22`, `C-52`.
  Mechanical, but real. Feed it `FINDINGS.md` rather than the raw bundle, the
  way `TEST-PLAN.md` describes.
- `C-03` (`/audiomodes` as a read) disagrees with itself. §17.1 still says
  CONFIRMED from the 2026-09-10 pass, the one deleted from this repo for
  leaking a real MAC. The clean 2026-09-11 re-run, same firmware and schema,
  came back INCONCLUSIVE: 200 OK with a 9-10 byte body on both players rather
  than the populated `<audiomode>` the vendor sample shows. Not a merge — find
  out why the body differs first.
- §17 carries two claim tables, topic-ordered and id-ordered, and they can
  drift. One has: the "Resolved on hardware" summary still says role reversal
  is "Rejected as a cycle; original master reasserts", which §5.3 disproves a
  few sections above it. Fix that row whether or not the append-only register
  lands.

Not in this PR — it is long enough. But a proposal nobody merges is a finding
nobody has.
